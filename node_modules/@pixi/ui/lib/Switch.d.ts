import { Container } from '@pixi/display';
import { Signal } from 'typed-signals';
/**
 * Container based component that switches visibility of containers by click.
 *
 * Can be used for creating tabs, radio buttons, checkboxes etc.
 * @example
 * ```
 * const switch = new Switch([
 *     new PixiSprite(Texture.from(`switch_off.png`)),
 *     new PixiSprite(Texture.from(`switch_on.png`)),
 * ]);
 *
 * ```
 */
export declare class Switch extends Container {
    /** TODO */
    view: Container<import("@pixi/display").DisplayObject>;
    /** TODO */
    views: Container[];
    /** TODO */
    activeViewID: number;
    /** TODO */
    onChange: Signal<(state: number) => void>;
    private button;
    constructor(views: Container[], activeViewID?: number);
    /** TODO */
    get activeView(): Container;
    /**
     * TODO
     * @param id
     */
    switch(id?: number): void;
}
//# sourceMappingURL=Switch.d.ts.map