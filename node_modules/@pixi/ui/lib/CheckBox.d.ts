import { Container } from '@pixi/display';
import { TextStyle, ITextStyle } from '@pixi/text';
import { Switch } from './Switch';
export type CheckBoxStyle = {
    checked: Container | string;
    unchecked: Container | string;
    text?: TextStyle | Partial<ITextStyle>;
};
export type CheckBoxOptions = {
    style: CheckBoxStyle;
    text: string;
    checked?: boolean;
};
/**
 * Creates a container based checkbox element
 * @example
 * ```
 *  new CheckBox({
 *     checked: false,
 *     style: {
 *         unchecked: new PixiSprite(Texture.from(`switch_off.png`)),
 *         checked: new PixiSprite(Texture.from(`switch_on.png`)),
 *     }
 * });
 *
 * ```
 */
export declare class CheckBox extends Switch {
    private label;
    constructor(options: CheckBoxOptions);
    /** TODO */
    update(): void;
    /** TODO */
    set text(text: string);
    /** TODO */
    get text(): string;
    /** TODO */
    get checked(): boolean;
    /** TODO */
    set checked(checked: boolean);
}
//# sourceMappingURL=CheckBox.d.ts.map